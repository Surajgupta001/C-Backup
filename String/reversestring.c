#include <stdio.h>
int main(){
    char str[40];
    puts("Enter a string");
    scanf("%[^\n]s",str);
    puts("The size of is :"); // Automatically \n bhi de deta hai
    // Reversing
    
}